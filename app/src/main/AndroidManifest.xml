<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
      SECURE (v2): android:debuggable is no longer hardcoded (AGP sets it per build
      type automatically - false in release, true in debug - instead of always true),
      and android:allowBackup is now false so `adb backup`/auto-backup can no longer
      pull aegis_vault.db or auth_prefs off the device. Fixes the v1 manifest flaws.
    -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectAegis">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          SECURE (v2): these are only ever launched from within this app via explicit
          Intents, so they no longer need android:exported="true". Setting it false
          fixes the v1 flaw where any other app on the device could start them
          directly (e.g. jump straight to the vault, bypassing MainActivity's PIN/
          biometric gate).
        -->
        <activity
            android:name=".VaultActivity"
            android:exported="false"
            android:label="@string/vault_title" />
        <activity
            android:name=".EntryFormActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".CredentialDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ChangePinActivity"
            android:exported="false" />
    </application>

</manifest>
