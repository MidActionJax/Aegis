<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/Aegis.Card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:clickable="true"
    android:focusable="true"
    app:rippleColor="@color/aegis_gold_container">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/avatarLetter"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_avatar_circle"
            android:gravity="center"
            android:textColor="@color/aegis_on_gold"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="G" />

        <TextView
            android:id="@+id/accountName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/aegis_on_bg"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/toggleVisibility"
            app:layout_constraintStart_toEndOf="@id/avatarLetter"
            app:layout_constraintTop_toTopOf="@id/avatarLetter"
            tools:text="GitHub" />

        <TextView
            android:id="@+id/username"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/aegis_on_bg_muted"
            android:textSize="13sp"
            app:layout_constraintEnd_toStartOf="@id/toggleVisibility"
            app:layout_constraintStart_toEndOf="@id/avatarLetter"
            app:layout_constraintTop_toBottomOf="@id/accountName"
            tools:text="jaxon@example.com" />

        <TextView
            android:id="@+id/passwordMasked"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="6dp"
            android:fontFamily="monospace"
            android:letterSpacing="0.1"
            android:textColor="@color/aegis_on_bg_muted"
            android:textSize="15sp"
            app:layout_constraintEnd_toStartOf="@id/toggleVisibility"
            app:layout_constraintStart_toEndOf="@id/avatarLetter"
            app:layout_constraintTop_toBottomOf="@id/username"
            tools:text="••••••••••" />

        <ImageButton
            android:id="@+id/toggleVisibility"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/action_toggle_visibility"
            android:src="@drawable/ic_visibility"
            app:layout_constraintEnd_toStartOf="@id/copyButton"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/copyButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/action_copy_password"
            android:src="@drawable/ic_copy"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
